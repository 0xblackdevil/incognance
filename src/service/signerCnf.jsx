// import { ethers } from "ethers";

// const _network = new ethers.Network("hardhat", 31337);

// const walletProvider = new ethers.BrowserProvider(window.ethereum, _network);

// const walletSigner = await walletProvider.getSigner();

// export default walletSigner;